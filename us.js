function setup() {
  // Crea il canvas con larghezza della finestra
  createCanvas(windowWidth, windowHeight);
}

function draw(){
    background("#26231d");
    textAlign(CENTER, CENTER)
    textSize(50);
    fill("#f0f0f0")
    text("Pagina su di noi", windowWidth/2, windowHeight/2); 
}